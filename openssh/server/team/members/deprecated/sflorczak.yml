parameters:
  linux:
    system:
      user:
        sflorczak:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/sflorczak
          name: sflorczak
  openssh:
    server:
      user:
        sflorczak:
          enabled: false
          user: ${linux:system:user:sflorczak}
