parameters:
  linux:
    system:
      user:
        miwinski:
          enabled: false
          name: miwinski
          full_name: disabled
          home: /home/miwinski
          email: disabled
  openssh:
    server:
      user:
        miwinski:
          enabled: false
          user: ${linux:system:user:miwinski}
