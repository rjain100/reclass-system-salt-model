parameters:
  linux:
    system:
      user:
        rkamaldinov:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/rkamaldinov
          name: rkamaldinov
  openssh:
    server:
      user:
        rkamaldinov:
          enabled: false
          user: ${linux:system:user:rkamaldinov}
