parameters:
  linux:
    system:
      user:
        vnogin:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/vnogin
          name: vnogin
  openssh:
    server:
      user:
        vnogin:
          enabled: false
          user: ${linux:system:user:vnogin}
