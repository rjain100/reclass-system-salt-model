parameters:
  linux:
    system:
      user:
        msamoylov:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/msamoylov
          name: msamoylov
  openssh:
    server:
      user:
        msamoylov:
          enabled: false
          user: ${linux:system:user:msamoylov}
