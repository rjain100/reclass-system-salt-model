parameters:
  linux:
    system:
      user:
        mniedbala:
          enabled: false
          name: mniedbala
          full_name: disabled
          home: /home/mniedbala
          email: disabled
  openssh:
    server:
      user:
        mniedbala:
          enabled: false
          user: ${linux:system:user:mniedbala}
