parameters:
  linux:
    system:
      user:
        jdiaz:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/jdiaz
          name: jdiaz
  openssh:
    server:
      user:
        jdiaz:
          enabled: false
          user: ${linux:system:user:jdiaz}
