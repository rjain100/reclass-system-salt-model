parameters:
  linux:
    system:
      user:
        okosse:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/okosse
          name: okosse
  openssh:
    server:
      user:
        okosse:
          enabled: false
          user: ${linux:system:user:okosse}
