parameters:
  linux:
    system:
      user:
        akuznetsova:
          email: disabled
          enabled: false
          full_name: disabled
          home: /home/akuznetsova
          name: akuznetsova
  openssh:
    server:
      user:
        akuznetsova:
          enabled: false
          user: ${linux:system:user:akuznetsova}
