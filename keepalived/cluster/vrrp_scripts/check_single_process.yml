parameters:
  keepalived:
    cluster:
      vrrp_scripts:
        check_pidof:
          args: ${_param:keepalived_vrrp_script_check_pidof_args}
          interval: 10
          rise: 1
          fall: 1
